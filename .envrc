export JJ_ALIAS_NAME="feature-done"
export JJ_ALIAS_CONFIG_FILE="jj-alias.toml"
export JJ_ALIAS_SMOKE_ARGS="--smoke"
export JJ_ALIAS_SUPPORT_FILE="codex_skill/bin/feature-done.nu"
export JJ_ALIAS_SUPPORT_TARGET="feature-done.nu"

export SKILL_NAME="$JJ_ALIAS_NAME"
export SKILL_DESCRIPTION="Run feature-done when @feature-done is explicitly referenced."
export SKILL_PURPOSE="finalize a jj feature stack into one polished commit on top of trunk"
export TOOL_DESCRIPTION="Run the jj feature-done alias when @feature-done is explicitly referenced."
export CRITICAL_CONSTRAINT="If the helper command fails, return its output to the user and stop."
