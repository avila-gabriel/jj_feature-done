version: "3"

tasks:
  bootstrap:
    desc: Verify jj tooling and alias configuration
    cmds:
      - sh scripts/bootstrap.sh

  done:
    desc: Validate alias config without touching user config
    cmds:
      - sh scripts/check.sh

  install:
    desc: Install the jj alias and Codex skill
    cmds:
      - sh scripts/install.sh
